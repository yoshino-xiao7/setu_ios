# iOS 问题登记与基线校正（2026-09-09）

## 基线错误及处理

前一轮误用 `yukiryou/restore-original-ui` 的 `d62fa4c` 加未提交改动构建并安装，导致后续图片详情与音乐更新没有进入安装包。该轮测试只证明旧基线自身的行为，不能作为最新版交付依据。

已核对远端主线为 `b94bd18`，本地 `227e995` 包含该主线并增加图片连续浏览更新。当前工作目录已对齐到 `227e995`，保留现有未提交品牌、开屏、模块动画与有效 UI 修复。没有新建第二套图片分页、缓存或转场，也没有替换最新版音乐播放引擎。

| 问题 | 校正后的实现 | 验证 |
| --- | --- | --- |
| 各页面顶部白带 | 原生 Tab 内容背景延伸到安全区 | 模拟器检查通过 |
| 图片详情重新加载、从下方弹出、详情样式回退 | 恢复已有 `ArtworkImageStore`、`ArtworkPager` 与原位缩放转场；保留最新版的横向翻页、长按与精简详情 | 缓存 3 项通过；无加载占位测试通过；430 深色交互通过；375 首轮点击未进入详情，单独复测通过 |
| 图片与 AI 绘画 logo 缺失 | 接入现有品牌工具栏，AI 删除记录收进更多菜单以避免小屏标题裁切 | 各模块 Logo、AI 更多菜单、小屏安全区检查通过 |
| 音乐不是最新版 | 恢复主线音乐首页、资料库、发现、播放器、缓存及播放状态改进；开发版本功能开关沿用最新版配置 | 跨页单播放器状态与控件检查通过；发现页和音频测试未全部通过，见下文 |

本地 `.local/ios-baseline-reconcile-20260909/` 保存合并前未提交内容快照与三方比对记录，便于核对保留项。此前 `.local/ios-bugfix-20260909/` 的安装回执仅代表已被识别为错误的旧版本。

基线校正阶段尚未创建提交或推送（之后的主线整合见下文）。重新安装前必须确认新构建来自 `227e995` 加校正后的未提交内容，并分别记录构建、模拟器、安装与启动结果。

## 本次验证及安装结果

- 真机 Debug 构建成功，版本 `1.1 (5)`；已安装到「雪涼的iPhone」并成功正常启动。
- 安装来源为 `227e995a9c75e2ed8505ee50e2ef941dcc6e05c6` 加保留的未提交改动；音乐播放引擎与该提交一致。
- `ArtworkImageStoreTests` 3 项、`NowPlayingCoordinatorTests` 2 项通过；图片打开连续性、明暗详情滑动/取消滑动/长按/关闭、模块 Logo 与状态栏、音乐跨页面单播放器、44 点控件分别通过。375 点详情首次点击失败，独立复测通过，保留两次日志。
- `MusicPlaybackControllerTests` 执行 34 项、跳过 1 项，出现 22 个失败断言；模拟器同时持续报告默认音频设备不存在。不能据此宣称音频功能验收通过，也未为通过测试而修改主线引擎。
- 音乐发现首页测试缺少预期内容，3 个断言/操作失败；尚未完成根因确认，未宣称该场景通过。真机正常启动不等同于音乐播放、线上数据及动效的完整人工验收。
- `git diff --check` 通过；这轮构建安装发生于主线整合提交之前。

证据位于 `.local/ios-baseline-reconcile-20260909/verification/`：源码清单、版本/二进制哈希回执、安装与启动 JSON、构建与测试日志、模拟器录屏。此前旧包回执不作为本次验收依据。

## 主线整合与后续会话

用户已要求将上述未提交内容并入 main。此次整合包含品牌、开屏、模块动画、有效 UI 修复，以及已恢复的图片连续浏览更新；保留上面未通过测试的记录。项目 `AGENTS.md` 和 `scripts/check-baseline.sh` 要求每次会话与构建安装前刷新、核验主线，禁止将旧恢复分支或旧包误认为最新版。最终合并提交及远端状态请查询实时 Git；本节不固定“永远最新”的 SHA。

整合前尝试 `swift test`：macOS SwiftPM 构建 App 目标时，`HomeWelcomeAnimation.swift` 的 UIKit 依赖无法解析，未执行到测试。iOS 真机与模拟器 Xcode 构建已在上轮通过；不将 SwiftPM 失败写成通过。
